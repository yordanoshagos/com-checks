import { Metadata } from "next";
import Waitlist from "@/features/waitlist/components/waitlist";

export const metadata: Metadata = {
  title: "Complēre Waitlist",
  description: "Complēre waitlist page.",
};

export default Waitlist;
